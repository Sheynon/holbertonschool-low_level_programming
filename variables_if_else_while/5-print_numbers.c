#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: Print all single digit number from 0 to 10.
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int b = 0;

	while (b < 10)
	{
		printf("%d", b);
		b++
	}
	printf("\n");
	return (0);
}
